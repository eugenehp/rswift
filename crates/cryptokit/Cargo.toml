[package]
name = "cryptokit"
description = "Apple CryptoKit — cryptography from Rust"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0"
repository = "https://github.com/eugenehp/rswift"

[lib]
name = "cryptokit"

# Uses CommonCrypto (part of libSystem) — no extra deps needed

[lints]
workspace = true
