//! ObjC selector constants for SpriteKit.
#![allow(dead_code)]

// ── SKScene (0 methods, 0 properties) ──
pub mod s_k_scene {
    pub const CLASS: &[u8] = b"SKScene\0";
}

// ── SKSpriteNode (0 methods, 0 properties) ──
pub mod s_k_sprite_node {
}

// ── SKLabelNode (0 methods, 0 properties) ──
pub mod s_k_label_node {
}

// ── SKShapeNode (0 methods, 0 properties) ──
pub mod s_k_shape_node {
}

// ── SKAction (8 methods, 0 properties) ──
pub mod s_k_action {
    pub const SEL_STEREO_PAN_TO: &[u8] = b"stereoPanTo:duration:\0";
    pub const SEL_STEREO_PAN_BY: &[u8] = b"stereoPanBy:duration:\0";
    pub const SEL_CHANGE_REVERB_TO: &[u8] = b"changeReverbTo:duration:\0";
    pub const SEL_CHANGE_REVERB_BY: &[u8] = b"changeReverbBy:duration:\0";
    pub const SEL_CHANGE_OBSTRUCTION_TO: &[u8] = b"changeObstructionTo:duration:\0";
    pub const SEL_CHANGE_OBSTRUCTION_BY: &[u8] = b"changeObstructionBy:duration:\0";
    pub const SEL_CHANGE_OCCLUSION_TO: &[u8] = b"changeOcclusionTo:duration:\0";
    pub const SEL_CHANGE_OCCLUSION_BY: &[u8] = b"changeOcclusionBy:duration:\0";
}

// ── SKPhysicsBody (0 methods, 0 properties) ──
pub mod s_k_physics_body {
}

// ── SKPhysicsWorld (0 methods, 0 properties) ──
pub mod s_k_physics_world {
}

// ── SKView (0 methods, 0 properties) ──
pub mod s_k_view {
}

// ── SKTexture (0 methods, 0 properties) ──
pub mod s_k_texture {
}

// ── SKEmitterNode (0 methods, 0 properties) ──
pub mod s_k_emitter_node {
}

// Total: 8 selector constants
