[package]
name = "rswift-roomplan"
description = "Alias for roomplan — re-exports the roomplan crate under the rswift- namespace"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0"
repository = "https://github.com/eugenehp/rswift"

[lib]
name = "rswift_roomplan"

[dependencies]
roomplan = { path = "../roomplan" }

[lints]
workspace = true
