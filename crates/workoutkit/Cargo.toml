[package]
name = "workoutkit"
description = "Apple WorkoutKit — workout composition from Rust"
version = "0.0.1"
edition = "2021"
license = "GPL-3.0"
repository = "https://github.com/eugenehp/rswift"

[lib]
name = "workoutkit"

[dependencies]
apple-sys-helpers = { workspace = true }

[lints]
workspace = true
